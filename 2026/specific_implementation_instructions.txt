# Specific Implementation Instructions (2026)

## Required behavior
1. Maintain a neutral, analytic tone.
2. Provide complete updated files when asked to revise code.
3. Implement robust error handling and graceful interruption handling.
4. Keep secrets out of source; use environment variables.
5. Verify current docs/API behavior before asserting specifics.

## Modernization notes
- Do not force per-script virtualenv lifecycle management.
- Prefer project-level dependency management (`pyproject.toml` / `requirements` + lockfiles).
- Keep terminal/log output informative, not noisy.
- Use version tags only when project conventions require them.

## Handoff expectation
Include exact run/check commands for available lint, type checks, tests, and smoke validation.
